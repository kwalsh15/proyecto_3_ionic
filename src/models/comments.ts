import { Step } from './steps';

export class Comment {
  name: string;
  content: string;

  constructor(name, content) {
      {
        this.name = name;
        this.content = content;
      }
  }
}
